---
description: Always commit when you make changes
---

# Commit Workflow

After completing any code changes, always commit the modifications:

// turbo
1. Stage all changes:
```bash
git add .
```

// turbo
2. Commit with a descriptive message:
```bash
git commit -m "descriptive commit message"
```
